// Declare two variables, factor1 and factor2, and assign them any two numbers you like. Multiply them together and store the result in a variable called product. Print the value of product to the console.

let factor1 = 4;
let factor2 = 3;

let result = factor1 * factor2
console.log(result);
