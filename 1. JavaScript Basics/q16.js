// Declare two boolean variables, isSunny and isWeekend, and assign them any boolean values (true or false).
// Use the logical AND operator (&&) to check if both variables are true, and the logical OR operator (||) to check if at least one of them is true.
// Print the results of both operations to the console.

let isSunny = true
let isWeekend = false

let andOperator = isSunny && isWeekend
let orOperator = isSunny || isWeekend

console.log(andOperator);
console.log(orOperator);

