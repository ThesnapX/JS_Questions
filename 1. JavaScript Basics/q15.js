// Declare two variables, x and y, and assign them the values 5 (as a number) and "5" (as a string) respectively.
// Use the equality operator (==) to compare them, and then use the strict equality operator (===) to compare them again.
// Print the results of both comparisons to the console.

let x = 5
let y = "5"

let ifValueEqual = x==y         //Check the vlaue and then compare
console.log(ifValueEqual);

let ifDataTypeEqual = x===y     //Check the data type
console.log(ifDataTypeEqual);

