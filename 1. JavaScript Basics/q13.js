// Declare a variable named score and set its value to 20.

    // Use the addition assignment operator (+=) to add 5 to score.

    // Use the subtraction assignment operator (-=) to subtract 3 from score.

    // Print the value of score to the console after each operation.


let score = 20 
score += 5
score -= 3
console.log(score);

